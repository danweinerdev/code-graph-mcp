//! Parallel filesystem discovery walker.
//!
//! Phase 3.2 fills this in. The placeholder exists so [`crate::server`] (and
//! anything else that wants to forward-reference the discovery layer) has a
//! stable module path to compile against.
