"""Type stub file. .pyi extension dispatches to the same parser as .py
and produces symbols identically — `def f() -> T: ...` is still a
function_definition with `...` as its body."""

def foo(x: int) -> str: ...

class Stub:
    def m(self) -> None: ...
    def n(self, x: int) -> int: ...

class Protocol:
    def required(self) -> bool: ...
